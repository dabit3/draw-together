import { Button as KButton, Input as KInput } from '@ui-kitten/components';
import { styled } from 'nativewind';
import { Image as RNImage, Text as RNText, View as RNView } from 'react-native';

export const Button = styled(KButton);
export const Input = styled(KInput);
export const Image = styled(RNImage);
export const Text = styled(RNText);
export const View = styled(RNView);
